# Strands AI Agent Demo - Dependencies
# Core agent framework with OTel integration
strands-agents[otel]==0.1.5
strands-agents-tools==0.1.1

# AWS SDK for Bedrock model access (credentials via node instance role/IMDS)
boto3>=1.34.0

# OpenTelemetry - HTTP-only export to ADOT collector
# strands-agents requires opentelemetry-api>=1.30.0
opentelemetry-api>=1.30.0
opentelemetry-sdk>=1.30.0
opentelemetry-exporter-otlp-proto-http>=1.30.0
